#ifndef __UTILS__H__
#define __UTILS__H__

#include <litc.h>
#define max(a, b) ((a) > (b) ? (a) : (b))

long long my_rdtsc();

void sort(double *, int);
int cmp_ll(const void *, const void *);

#endif
